<%= render 'comm/top' %>


<p>
  <strong>Title:</strong>
  <%= @access.title %>
</p>

<p>
  <strong>Code:</strong>
  <%= @access.code %>
</p>

<%= link_to 'Edit', edit_access_path(@access) %> |
<%= link_to 'Back', accesses_path %>
