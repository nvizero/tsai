<h1>Listing product_verify_contents</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @product_verify_contents.each do |product_verify_content| %>
      <tr>
        <td><%= link_to 'Show', product_verify_content %></td>
        <td><%= link_to 'Edit', edit_product_verify_content_path(product_verify_content) %></td>
        <td><%= link_to 'Destroy', product_verify_content, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product verify content', new_product_verify_content_path %>
