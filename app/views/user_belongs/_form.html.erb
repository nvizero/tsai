<%= form_for(@user_belong) do |f| %>
  <% if @user_belong.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_belong.errors.count, "error") %> prohibited this user_belong from being saved:</h2>

      <ul>
      <% @user_belong.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :belong_user_id %><br>
    <%= f.text_field :belong_user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
